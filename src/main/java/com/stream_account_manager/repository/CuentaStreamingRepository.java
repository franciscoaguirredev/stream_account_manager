package com.stream_account_manager.repository;

import com.stream_account_manager.model.CuentaStreaming;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CuentaStreamingRepository extends JpaRepository<CuentaStreaming, Long> {
}
